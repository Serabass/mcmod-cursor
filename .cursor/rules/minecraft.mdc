---
alwaysApply: true
---

Запускай игру в обычном режиме (не в фоновом), чтобы видеть логи
Обращайся к документации https://docs.fabricmc.net/ за решениями

Перед билдом прогоняй тесты и исправляй, если они не прошли.

Сборку и тесты запускай без докера

Выводи при тестах и билдах только ошибки, чтобы сократить количество информации

Для генерации заглушек текстур используй обращение (Invoke-WebRequest) к dummyimage.com

При запуске команд используй только PowerShell подход
